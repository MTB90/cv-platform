# Platform Backend

Install dependencies:
```shell script
    make pip-install
    make pip-install-test
```

## Local development:

Start dependencies services:
```shell script
    make up-services
```

Serve backend:
```shell script
    make serve
```